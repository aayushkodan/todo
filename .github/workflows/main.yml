name: todo-workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:

      - name: Run CI pipeline
        run: |
          echo "Starting CI/CD pipeline for todo-backend"
          echo "Repository checked out successfully"
